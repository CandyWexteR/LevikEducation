namespace EntitySamples;

public static class Program
{
    public static void Main(string[] args)
    {
        var list = new SelfList();

        // list.Count = -1;
        var count = list.Count;
        
    }
}